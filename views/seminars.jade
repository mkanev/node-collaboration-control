extends layout

block content
    ul.thumbnails
        each item in items
            li.span4
                .thumbnail
                    a(href='/seminar/#{item.id}')
                        img(src='http://placehold.it/400x270')
                        h4 #{item.title}
                        span.caption(style='text-align: justify;') #{item.text}